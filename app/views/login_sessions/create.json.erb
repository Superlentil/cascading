{
  <% if !cookies[:user_id].nil? %>
    "user_id": <%= cookies[:user_id] %>,
    "user_nickname": "<%= cookies[:user_nickname] %>",
    "user_avatar_url": "<%= cookies[:user_avatar_url] %>",
  <% end %>
  
  <% if !cookies[:user_login_status].nil? %>
    "user_login_status": "<%= cookies[:user_login_status] %>"
  <% end %>
}
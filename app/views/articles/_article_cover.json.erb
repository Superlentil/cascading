{
  <% unless articleCover.nil? %>
    "id": <%= articleCover.id %>,
    "title": <%= raw(articleCover.title.to_json) %>,
    "cover_picture_id": <%= articleCover.cover_picture_id %>,
    "cover_picture_url": <%= raw(articleCover.cover_picture_url.to_json) %>,
    "cover_picture_height": <%= articleCover.cover_picture_height %>,
    "abstract": <%= raw(articleCover.abstract.to_json) %>,
    "like": <%= articleCover.like %>
  <% end %>
}
{
  <% unless user.nil? %>
    "id": <%= user.id %>,
    "email": <%= raw(user.email.to_json) %>,
    "nickname": <%= raw(user.nickname.to_json) %>,
    "avatar_url": <%= raw(user.avatar.url(:thumb).to_json) %>,
    "status": <%= user.status %>
  <% end %>
}